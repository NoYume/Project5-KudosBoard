# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Local Homebrew default (no password, user = your macOS username):
DATABASE_URL="postgresql://USER@localhost:5432/kudos_board?schema=public"
